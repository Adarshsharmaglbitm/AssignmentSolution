class Solution {
    public void moveZeroes(int[] nums) {
       //Arrays.sort(nums);
       int k=0;
      for(int i=0;i<nums.length;i++){
          if(nums[i]!=0){
              int t=nums[k];
              nums[k]=nums[i];
              nums[i]=t;
             k++; 
         }
      }  
    }
}
